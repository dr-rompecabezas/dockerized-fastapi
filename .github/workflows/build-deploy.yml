name: GitHub Actions Build and Deploy
on: [push, pull_request]
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: pulling git repo
        uses: actions/checkout@v2
      - name: ci/cd - hello
        run: echo "my first ci/cd pipeline"
